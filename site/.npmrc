# Turn the `engines` field in package.json into a hard error instead of the
# warning npm prints by default, so an unsupported Node version fails at
# `npm install` with one clear line rather than as a pile of syntax errors
# from `npm test` (which parses TypeScript directly) or an opaque Astro build
# failure much later.
engine-strict=true
